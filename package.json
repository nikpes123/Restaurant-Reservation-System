{
    "scripts": {
        "dev": "concurrently --kill-others-on-fail \"cd backend && npm run dev\" \"wait-on http://localhost:5000 && cd frontend && npm run dev\""
    },
    "devDependencies": {
        "wait-on": "^8.0.2"
    },
    "dependencies": {
        "@react-google-maps/api": "^2.20.6",
        "cors": "^2.8.5",
        "multer": "^1.4.5-lts.2"
    }
}
